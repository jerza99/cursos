
    let user = {
        id: 1,
        name: "Jerza",
        age: "26",
    };

    for (let prop in user) {
        console.log(prop, user[prop]);
    }

    let animales = ['gato','perro','dragon'];

    for (let indixe in animales) {
        console.log(animales[indixe]);
    }